export const selectDebate = (state) => state.debate;
export const selectEstado = (state) => state;
export const selectCandidatos = (state) => state.candidatos;
export const selectDescripcion = (state) => state.debate;
export const selectNombre = (state) => state.name;
export const selectVotacion = (state) => state.votacion;
